import { DefaultTheme } from 'styled-components';

const sayHiRepoTheme: DefaultTheme = {
  borderRadius: '5px',

  colors: {
    main: 'cyan',
    secondary: 'magenta',
  },
};

export { sayHiRepoTheme };